/**
 * Script for WhatsApp Order Button
 * Keeps dynamic DOM clean
 */
document.addEventListener('DOMContentLoaded', function() {
    // We don't necessarily need to attach JS events if simple links suffice
    // But this file is enqueued if we need dynamic behavior (like updating variable product links) in future versions.
});
